using System;
using UnityEngine;
using ZeroSDK.UIBuilder.AddOns.Button;
using ZeroSDK.UIBuilder.Core;
using ZeroSDK.UIBuilder.Core.UIElements;

namespace _project.Scripts
{
    public sealed class hrwehwewegwegweg : thygtfrdsd
    {
        [SerializeField] private rewgrehtrn playButton;
        [SerializeField] private rewgrehtrn levelsButton;
        [SerializeField] private rewgrehtrn bonusButton;
        [SerializeField] private rewgrehtrn optionsButton;
        [SerializeField] private rewgrehtrn exitButton;
        
        public event Action OnPlayButtonEvent
        {
            add => playButton.hehreherh += value;
            remove => playButton.hehreherh += value;
        }
        
        public event Action OnLevelsButtonEvent
        {
            add => levelsButton.hehreherh += value;
            remove => levelsButton.hehreherh += value;
        }

        
        public event Action OnBonusButtonEvent
        {
            add => bonusButton.hehreherh += value;
            remove => bonusButton.hehreherh += value;
        }

        
        public event Action OnOptionsButtonEvent
        {
            add => optionsButton.hehreherh += value;
            remove => optionsButton.hehreherh += value;
        }
        
        public event Action OnExitButtonEvent
        {
            add => exitButton.hehreherh += value;
            remove => exitButton.hehreherh += value;
        }
        
        protected override void ewregtrhnht() => qewrfegtrfhng.ewregtrh.ewregthrnhg<BackgroundScreen>().ShowBlur();
    }
}